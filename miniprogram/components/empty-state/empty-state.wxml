<view class="empty-state">
  <view class="empty-icon">
    <text>{{icon || '📭'}}</text>
  </view>
  <view class="empty-title">{{title || '暂无数据'}}</view>
  <view class="empty-desc" wx:if="{{description}}">{{description}}</view>
  <van-button type="primary" size="small" round 
              wx:if="{{buttonText}}" 
              bind:click="onAction"
              custom-style="background: #2FA866; border-color: #2FA866; margin-top: 24rpx;">
    {{buttonText}}
  </van-button>
</view>
